extends ../base.pug
include ../mixins/music.pug

block section
    each user, index in users 
        span= users[index].username
        .music-grid
            each music in users[index].musics 
                +music(music)
            else
                span.empty__message 음악을 찾을 수 없습니다.
    else
        span.empty__message 유저를 찾을 수 없습니다.
